#include "UnitTest.h"
/*
int fake_test(int a)
{
	return (a);
}

int test(int a)
{
	if(a)
		exit(1);
	else
		return (0);
}
		
void exit_test(void)
{
	int (*original_exit)(int) = exit;
	exit = fake_test;
	CU_ASSERT_EQUAL(1, test(1));
	CU_ASSERT_EQUAL(1, test(1));
	CU_ASSERT_EQUAL(1, test(1));
	CU_ASSERT_EQUAL(0, test(0));
	CU_ASSERT_EQUAL(0, test(0));
	CU_ASSERT_EQUAL(0, test(0));
	exit = original_exit;
}
*/
